--развернуть виртуальную машину любым удобным способом

-- развернута вм на вдсина.ру 
-- Процессор: 2 core   Память: 4 Gb   Хранилище: 100 Gb, по идее ссд

--поставить на неё PostgreSQL 15 любым способом

```sudo apt install -y postgresql-15 postgresql-client-15```

--настроить кластер PostgreSQL 15 на максимальную производительность не обращая внимание на возможные проблемы с надежностью в случае аварийной перезагрузки виртуальной машины

```shared_buffers = 1GB
effective_cache_size = 3GB
work_mem = 57MB
maintenance_work_mem = 205MB

min_wal_size = 2GB
max_wal_size = 3GB
checkpoint_completion_target = 0.9
wal_buffers = -1

listen_addresses = '*'
max_connections = 25

random_page_cost = 1.1
effective_io_concurrency = 200

max_worker_processes = 8
max_parallel_workers_per_gather = 2
max_parallel_workers = 2

temp_buffers = 32MB

join_collapse_limit = 16
default_statistics_target = 200
track_activity_query_size = 4096
geqo = on

--нагрузить кластер через утилиту через утилиту pgbench (https://postgrespro.ru/docs/postgrespro/14/pgbench)
--написать какого значения tps удалось достичь, показать какие параметры в какие значения устанавливали и почему

```в основном параметры взяты из, на сайты вбил мои парамтры вм и нагрузку олтп
https://www.pgconfig.org/
https://pgconfigurator.cybertec.at

+ 4 парамтра ниже гугл + чатгпт
join_collapse_limit
default_statistics_target
track_activity_query_size
geqo
 
sudo -u postgres pgbench -c 10 -j 2 -T 60 pgbench
tps = 3188.642610

sudo -u postgres pgbench -c 25 -j 2 -T 60 pgbench
tps = 2362.340936

при включенных (закомментированных в конфиге)
fsync
synchronous_commit

sudo -u postgres pgbench -c 25 -j 2 -T 60 pgbench
tps = 623.188420


--Задание со *: аналогично протестировать через утилиту https://github.com/Percona-Lab/sysbench-tpcc (требует установки
https://github.com/akopytov/sysbench)

```заплутал и не разобрался)
